"""
Owns: purchase order source adapter — scrapes the order management system.
Must not: import services; must not hardcode credentials or paths.
May import: core.ports, core.errors, config, playwright, selenium, logging.
"""
